/** Placeholder — settings UI ships later. */
export function SettingsPage() {
  return (
    <div className="page" style={{ padding: "1.5rem" }}>
      <h1>Settings</h1>
      <p>Organization settings UI comes in a later phase.</p>
    </div>
  );
}
