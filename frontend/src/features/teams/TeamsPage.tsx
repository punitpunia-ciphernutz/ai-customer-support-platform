/** Placeholder — teams management UI ships later. */
export function TeamsPage() {
  return (
    <div className="page" style={{ padding: "1.5rem" }}>
      <h1>Teams</h1>
      <p>Team APIs are available; management UI comes in a later phase.</p>
    </div>
  );
}
