/** Placeholder — tickets UI ships after Day 2. */
export function TicketsPage() {
  return (
    <div className="page" style={{ padding: "1.5rem" }}>
      <h1>Tickets</h1>
      <p>Ticket APIs are available; full UI comes in a later phase.</p>
    </div>
  );
}
